# 凭证值识别：纠正采用报告的错误阻断

日期：2026-09-10。目标用户是沿 MCP 选型指南查看 GitHub MCP 报告的开发者；本次改善报告的证据可靠性，不以升分或发版代替获客。

## 基线与真实阻碍

02:46 生产日报：D7 新注册与首次评测用户均为 0，页面浏览 45、评测 CTA 4；已有账号完成一次新评测，不能称为新增客户。指南和 GitHub 入口观察日仍为 9 月 11 日、16 日。

公开 GitHub MCP 报告生成于 9 月 7 日 01:31 UTC，版本 3.11.0，总分 39、critical/blocked。唯一静态安全告警为 `hardcoded-credential`，位置 `Dockerfile:33`、中置信度。报告没有保存源 commit，不能声称完整原始输入已被归档。

读取报告生成前最近修改 Dockerfile 的[官方固定提交](https://github.com/github/github-mcp-server/blob/2de6e4742bc44b1fdfd7337dc36541cc89d8a73f/Dockerfile#L33)：这行从 `/run/secrets/` 读取构建输入，带空值回退，源码里没有该凭证的字面值。固定源文件的该行与金标首例逐字符核对通过；完整 Dockerfile 的基线扫描复现同位置告警。只读取源码，不运行构建、不使用任何真实凭证。

[Docker 官方 secret 文档](https://docs.docker.com/build/building/secrets/)说明挂载文件的使用方式；[Dockerfile ENV 说明](https://docs.docker.com/reference/dockerfile/#env)与 shell 命令执行不同。因此不能把任意 `$(...)` 字符串或 Dockerfile 的 ENV/ARG 都当作安全注入。

假设：仅把有明确非字面值证据的读取表达式从“硬编码凭证”中排除，同时收紧占位符匹配范围，可减少错误阻断且不让相邻注释/占位符掩盖真实字面值。固定对照发生退化即停止发布；上线后核实目标报告，不泛化为整个项目安全或已提高用户转化。

## 实现范围

- 匹配成对引号，避免单引号嵌在双引号中截断识别。
- 占位符仅检查完整被赋值内容，不再检查整行。逐个检查同一行的候选，不让第一个占位符遮住后续凭证；保留同文件/行/规则去重。
- 仅识别 shell 文件中的赋值，以及 Dockerfile 的 RUN/export 赋值；完整双引号值必须是简单的 `/run/secrets/<name>` 文件读取，可带 stderr 丢弃或空串回退。非空回退、额外命令、路径穿越、字面前后缀、单引号字符串、ENV/ARG 和其他语言不豁免。
- 凭证证据仅保留脱敏后的匹配赋值；脱敏也匹配成对引号。其他 provider key、私钥、注入和危险命令规则不减弱。
- 引擎版本 3.14.0；评分权重、风险上限、AI prompt、认证、依赖与数据库结构不变。

这不是完整 shell/语言解析器或秘密泄漏审计。短于既有 6 字符阈值、无完整结束引号等仍属既有启发式边界。即使没有源码字面凭证，也必须另外核对 secret 是否进入构建产物/日志；本次不证明该构建或托管服务安全。

## 固定验证

金标集 `data/credential-value-cases.ts` v1.0.0，36 项，最终 SHA-256 `109359a203ac5731009008bec8fe55370daa7f09dcfdd618b220ce6f0eb40f34`。首例来自固定源表达式，其余是原创合成对照，没有真实密钥。

基线取 main `7e5d925` 的 scanner/redaction；候选取本次代码。每例重复 10 次，50 轮测单样本耗时。基线 24/36，候选 36/36，修复 12 项、既有通过项退化 0，两者输出漂移 0；本机中位均约 0.002ms、p95 均约 0.004ms，不作为生产延迟 SLA。新增模型调用和网络调用均为 0。

完整固定 Dockerfile：基线安全扫描分 76、critical、1 个凭证告警；候选安全扫描分 100、low、0 告警。**这只是该文件的静态模式结果，不是项目总分或安全认证。** 另测 Dockerfile ENV/ARG、字面 shell 文本、非 shell 文件、provider key 保留和 200,000 字符值的边界。

复现：`SCANNER_BENCHMARK_BASELINE=<旧 scanner 模块的绝对路径> node --import tsx scripts/benchmark-credential-values.ts --check`。旧模块的相对 imports 需指向其对应依赖；扫描行为与脱敏必须一起冻结。只验证候选时省略环境变量。

初次候选虽然分类通过，新增脱敏断言发现嵌套引号后的合成凭证残留，已撤回 scanner/version 运行时改动并核对与 HEAD 无差异，随后修复脱敏再完整重验。后续扩大 Dockerfile 边界时，一条旧夹具缺少合法 export 上下文，导致额外告警；修正为实际赋值语句，保留 provider key 必须拦截的断言。未上线失败候选。固定源码的 raw 下载曾超时，换官方 Contents API 完成同一读取，不绕过访问控制。

本机 typecheck、lint、完整评测回归、榜单、文档格式/采用/输出三组基准与生产 build 通过。生产发布和目标报告的实际结果另记当日日报；本记录不预先声明已部署或报告已更新。

## 下一检查点

只发布已验证的隔离修复，保留未配置注册保护的隔离；验证目标报告的实际安全告警、图示、版本与可读性。旧报告不会因代码发布自动变更，不能只上线扫描器就宣称用户看到的错误已消失。正常运营指标和此次纠错评测分开记录，不批量重评库存，不改原获客观察窗口。
