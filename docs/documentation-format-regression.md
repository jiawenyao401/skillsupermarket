# README 代码证据识别回归（2026-09-06）

## 问题与范围

3.10.0 使用三反引号正则识别 README 安装/使用证据，导致等价的合法 Markdown 写法被漏判，也可能将 HTML 注释或样式块中的隐藏命令计为证据。

3.11.0 改为一次 CommonMark 语法树解析，只检查 `code` 节点，复用已在依赖树中的 `mdast-util-from-markdown@2.0.3`，将其声明为直接依赖。评分权重、风险封顶、置信度公式、模型提示词均不变。它不执行文档中的命令，不请求网络，不增加模型调用。

## 固定样本和结果

基线提交：`739ddce`；基线评测器：3.10.0；候选：3.11.0。
格式集 `data/documentation-format-cases.ts` 版本 1.0.0 从现有原创成熟金标派生，仅替换代码示例的展示语法。

| 指标 | 基线 | 候选 |
| --- | ---: | ---: |
| 12 个固定格式/负例通过数 | 5 | 12 |
| 5 个合法写法漏判 | 5 | 0 |
| 2 个隐藏 HTML 命令误判 | 2 | 0 |
| 同输入重复 10 次漂移 | 0 | 0 |
| 每 README 中位耗时（毫秒） | 0.010 | 0.166 |
| 每 README p95 耗时（毫秒） | 0.015 | 0.210 |
| 额外模型调用 | 0 | 0 |

同进程、50 轮预热后批量本地计时，中位增加 0.156 毫秒；这是固定短 README 的 CPU 微基准，不代表生产端到端延迟。不同机器/负载下数值会变化。该固定集不是对全部真实仓库准确率的估计。

改善项：波浪线围栏、长波浪线围栏、缩进代码、引用内代码、带元信息围栏；隐藏 HTML 注释和 style 块不再贡献采用证据。原本通过的 5 个对照没有退化。原评分金标 1.5.1、风险封顶和置信度测试全部保持通过。

## 复现与验证

先将基线 `lib/evaluation-scoring.ts` 从指定提交导出到临时目录，然后执行：

```sh
SCORING_BENCHMARK_BASELINE=/absolute/path/to/baseline.ts npm run benchmark:documentation
npm run typecheck
npm run lint
npm run test:evaluation
npm run test:rankings
npm run build
```

主分支测试：114 项，113 通过，1 项 Linux 专属用例在 macOS 跳过；榜单测试 3/3；类型、lint、生产构建均通过。新增异常/边界覆盖：空文档、CRLF、不闭合/不匹配围栏、非法信息字符串、行内伪围栏、约 32KB 分隔符文本和约 261KB 普通文本，重复输出一致且分数有界。tsx 在受限沙箱创建 IPC 管道被拒绝时，须使用获准的正常本机环境验证，不降低检查。

## 边界与发布

- 仅将 Markdown `code` 节点视为代码证据；原始 HTML `<pre>` 不在本次支持范围。
- 通用关键词/章节判断仍使用原有规则；本改动不是完整提示注入或文档语义真实性证明。
- 评测器版本递增，已有报告不会就地篡改；后续评测采用新版本。上线后检查报告生成和固定集，不批量消耗模型额度。
- 主分支含未配置服务的注册验证版本 `739ddce`。本次发布从当前生产 `b446fa8` 隔离应用评分提交，禁止夹带注册验证改动。无数据库结构变更，保留旧 release 回滚。

### 生产验收（2026-09-07 北京时间 09:32）

- 主分支实现提交 `d65ba56`；从生产基线隔离 cherry-pick 后的发布提交 `15b915e`，源码压缩包 SHA-256 为 `3db122150448f1aba2552a6d7698536adbbee332b1878dd89a9fd888051eaa88`。
- PostgreSQL 自定义格式备份 1,838,377 字节，`pg_restore --list` 校验通过；旧 release 完整保留，无数据库迁移。
- 实际 Node 20.20.2 服务器：类型、lint、99/99 完整测试（含 Linux 用例）、3/3 榜单测试、12/12 格式回归、生产构建通过。服务器固定集本地 CPU 中位 1.561ms、p95 3.498ms；不同硬件结果不能与开发机基线直接相减。
- 原子发布到 `/opt/releases/skillsupermarket-20260907-15b915e`。Web 和 Worker 的实际工作目录均为该 release；健康接口返回 3.11.0，数据库/Judge ready；SEO 检查、登录及权限保护、采集定时器通过。
- 一次运营冒烟由真实 Worker 完成（attempt=1），报告版本 3.11.0，并生成结构图；未扣用户额度。该端到端冒烟使用了配置的模型，与上表零模型调用的离线格式基准分开统计。
- 仅更新已验证的代码哈希基线，未改账号、公钥或端口基线；安全复检 healthy。发布后磁盘 47%，旧版本和备份保留。
- 推送命令两次在本机权限审批环节超时，未确认推送成功；已上线不等于 GitHub main 已同步。下一轮首先核对远端，避免重复或覆盖其他提交。

依据：[CommonMark 0.31.2 代码围栏规范](https://spec.commonmark.org/0.31.2/#fenced-code-blocks)、[解析器官方文档](https://github.com/syntax-tree/mdast-util-from-markdown)。
