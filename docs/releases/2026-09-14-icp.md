# 2026-09-14：全站网站备案号

## 范围与结果

- 采用站点所有者确认的网站备案号 `冀ICP备2026036754号-2`，不是缺少网站后缀的主体号。
- 全站共享页脚服务端直出备案链接，目标为 `https://beian.miit.gov.cn/`，新窗口使用 `noopener noreferrer`。
- 保留隐私入口；窄屏自动换行，备案号自身不拆行，键盘焦点可见。没有增加运行时请求、依赖或数据库变更。
- 备案号来自所有者确认；本次没有宣称独立完成工信部登记信息查询。

## 版本与验证

- main 功能提交：`e927bd0f0b5f9dea24748ce83494d2eea57aaf23`，已推送 GitHub，非强制更新。
- 生产候选基于原生产提交 `d6dd95501d27a03d0b99f91da2984f3c02364817`，仅拣选上述三文件改动，发布提交 `e576c2c7add3336c2e234a625a07021917bba7de`。
- main：typecheck、lint、build 通过；测试 158 通过、1 项 Linux-only 跳过、0 失败。本机 build 仍输出既有 BetterAuth 默认 secret 提示；未修改密钥，也不据此声称本机认证已验收。
- 生产候选：typecheck、lint、144/144 测试、只读 PostgreSQL 来源检索回归、SDK build、生产 build、依赖文件校验全部通过。生产认证文件、数据库结构、依赖清单及环境文件逐字节保持不变，不带入 main 上尚未启用的注册验证服务。
- 隔离预览及发布后的公网各通过 32 组浏览器检查：320/390/768/1280 像素，首页/评测服务/真实报告/登录页，JavaScript 开启与关闭。备案文字、HTTPS 目标、安全属性、可见性、裁切和溢出均通过，浏览器异常为零；已目视检查截图。
- 可复现检查：`PLAYWRIGHT_MODULE=/已有安装路径/playwright node scripts/check-icp-footer.mjs`。可通过 `BASE_URL` 指定候选版本。脚本拦截所有非 GET/HEAD 请求及统计入口，不创建账号、评测或流量数据。

## 上线验收与回滚

- 北京时间 2026-09-14 13:38:54 激活 `skillsupermarket-20260914-icp`；保留前一 `skillsupermarket-20260913-source` release 和新生成的数据库备份。备份 2,360,381 字节，`pg_restore --list` 校验通过。
- 激活前队列空闲；Web/Worker 均 online，实际进程目录与新 release 一致。首页、health、来源查找、真实报告、评测服务、登录页均 HTTP 200；数据库与 Judge ready；离线合成评测冒烟及 `seo:check` 通过。未发起收费模型请求或虚假线上评测。
- 采集 timer active，最近一次采集 service success/0；安全 timer active。13:36:39 安全定时检查记录了一次 `LISTENING_PORTS_CHANGED`，与仅绑定回环地址的临时预览时段一致；预览随后关闭，13:38:54 和 13:40:05 的安全复查均 healthy。没有删除告警、重置历史状态或改变账号/公钥/监听基线。
- 仅接受已验证 release 的代码基线：`84d1f3081403d71375144001d19309ba5ee8b586aae34e4a60dd7b0adefaa69a`，379 个文件；其余保护基线哈希不变。
- 部署后磁盘 88%，可用 4,926,948 KiB，满足现有 4 GiB 保留门槛。本次没有清理旧 release、备份或用户数据。
- 激活脚本保留失败自动回退路径；无功能、健康或浏览器回归，不需要回滚。下一次常规定时巡检继续检查现有服务和代码基线。
