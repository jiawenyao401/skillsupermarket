# 2026-09-14：Google Search Console 验证

## 已交付

- 所有者提供的 HTML 验证文件永久加入 `public/google4c98f5b1fa46b846.html`，公网路径为 `https://skillsupermarket.com/google4c98f5b1fa46b846.html`。
- 文件与上传原件逐字节相同：53 字节、无末尾换行，SHA-256 为 `5fc11eb42d098816a5fbe5db7f88c401875ae19ee7544120c831beb38afb2677`，未套入页面布局或修改内容。
- 根布局 Metadata 同时加入所有者提供的 Google meta token，保留环境变量配置的其他验证 token，不删除已有验证方式。
- 不修改生产环境文件、依赖、认证、模型、数据库结构或 ICP 备案页脚；网站 meta token 是有意公开的所有权证明，不是 API 凭据。

## 提交与验证

- main 功能提交 `dfcd26ea2821d5990f1c58f9027012708a312796`，已非强制推送 GitHub。
- 生产从 `e576c2c7add3336c2e234a625a07021917bba7de` 仅拣选本次三个文件，发布提交 `588ef82ea0759937abb70eab9ed625b27bf35ebc`。不带入尚未启用的注册发信服务改动。
- 本地 typecheck、lint、build 通过，159 项测试通过、1 项 Linux-only 跳过、0 失败。构建仍输出既有 BetterAuth 默认 secret 提示，未隐藏，不能据此声称本机生产认证已验收。
- 生产候选 typecheck、lint、145/145 测试、只读 PostgreSQL 来源回归、SDK build、生产 build、全部依赖文件哈希检查通过；旧版与候选源码清单分别 248/249 个文件匹配 Git。
- 预览与公网均验证：匿名 GET/HEAD 验证文件返回 200、无跳转、text/html，响应字节与原件相同；普通和 Google 验证样式 User-Agent 均通过。
- 首页原始 `<head>` 中包含正确 meta token，且在前 2 MB 内；浏览器 JavaScript 开启/关闭均可读取文件和 head 标签，浏览器异常为零；备案链接仍存在。
- 浏览器检查拦截写请求与统计入口，没有创建账户、评测、订阅或虚假流量。

## 发布与安全护栏

- 北京时间 2026-09-14 14:20:04 激活 `skillsupermarket-20260914-google`；上一 `skillsupermarket-20260914-icp` release 保留，失败自动回退路径可用。
- 发布前新建数据库备份 2,360,396 字节，`pg_restore --list` 校验通过。环境文件逐字节不变。
- 按既有资源维护授权，只删除了更早 `skillsupermarket-20260913-source/.next/cache/webpack` 的可再生编译缓存 334,880 KiB（约 327 MiB）；精确路径经检查，不是软链接。该版本运行文件、BUILD_ID、备份及业务数据未删除，缓存可通过重新构建生成。
- 激活前队列空闲；Web/Worker 均 online 且实际目录正确。health/首页/来源查找/真实报告/评测服务/登录页均 200；数据库/Judge ready、离线 SDK 冒烟和 seo:check 通过；采集与安全 timer active，最近采集 service success/0。
- 仅接受新代码基线 `1318ba7e26942a593a532d1084ca6efd583e363638647261df79d758cf8a8333`（380 个文件）；账号、公钥和监听基线哈希未改。
- 14:19:17 的定时端口变化告警与临时回环预览时段重合；发布后一次检查仍报端口差异。随后只读核实未发现持续差异，14:24:33 复查 healthy；未删除告警、重置历史状态、改动防火墙或接受新的监听基线。该复查不等于完整安全审计。
- 部署后磁盘 88%，可用 4,921,420 KiB，原有 4 GiB 保留门槛没有降低。

## 控制台下一步

用户在 Google Search Console 对 `https://skillsupermarket.com/` 点击“验证”。本次已完成网站侧部署，但没有替用户点击控制台验证，也不宣称所有权验证、站点地图提交或搜索收录已经完成。

按 [Google 官方验证说明](https://support.google.com/webmasters/answer/9008080#html_verification)，两种验证方式可以并存；验证后仍需长期保留。后续发布应保留上述文件和标签；若回滚到缺少它们的旧版本，应及时恢复，避免长期影响验证状态。
