# 2026-09-26 日报（北京时间）

## 唯一主瓶颈与可证伪假设

目标用户是从搜索进入具体 Skill 报告、希望判断项目能否采用的开发者。约 01:20 从生产聚合报表读取：D7 Skill 详情页浏览 24（粗粒度自然搜索来源 11），详情页评测 CTA 0、全站评测 CTA 0。首屏“查看评测依据”埋点只上线约 5 小时，本日及 D7 点击暂为 0；不能据此判定上一轮首屏改动失败。页面浏览不等于独立访客。

同一假设的承接环节：公开报告首屏原来只有查证据入口，想评测自己项目的人需另找全站入口。增加不抢夺证据主按钮的“评测我的项目”文本链接，沿用已有 `evaluation_cta_click` 聚合计数。观察 7–14 天详情页浏览、报告入口、评测 CTA、首次评测；若有足量真实详情访问而两种点击仍为 0，停止增加 CTA，转向真实访客访谈与搜索意图核查。无对照流量和转化样本前不宣称提升。

## 真实 D1 / D7 / D30 基线

数据来自生产 `growth:report`，约 01:20 北京时间。站内事件可能包含未识别的内部访问，不是独立用户数，也不能与 Google Search Console 的展示、点击或收录混为一谈。

| 指标 | D1 | D7 | D30 |
| --- | ---: | ---: | ---: |
| 新增账号 | 0 | 0 | 0 |
| 页面浏览 | 1 | 63 | 225 |
| 评测 CTA 点击 | 0 | 0 | 6 |
| 指南浏览 | 0 | 0 | 5 |
| 报告入口点击（新埋点） | 0 | 0 | 0 |
| 真实用户评测任务 | 0 | 0 | 11 |
| 新增 Skill | 0 | 5 | 23 |

- 账号总数 1；登录/注册页浏览 D1/D7/D30 为 0/0/7、评测页浏览为 0/3/11；D7 首次评测用户 0、重复评测用户 0、7 日活跃评测用户 0。D30 归因用户 1、重复评测用户 1，但首次评测用户 0，属于报表窗口口径，不推断新增激活。D7 无真实用户任务完成率样本；D30 用户任务 11/11 完成、失败 0；运维评测任务 D7 为 6，不混入用户任务。
- D7 覆盖调度 5 次且全部完成，失败 0；免费额度使用者 0、耗尽者 0、有效订阅 0。
- 有效 Skill 201；今日新增 0；约 04:41 复核时今日已采集 149，最近采集日期 2026-09-26（凌晨 01:20 首次报表时尚未开始当天采集，不是异常）；报告覆盖 190/201（94.5%），累计报告 212。
- 当前评测器 v3.16.0 的最新报告覆盖 7/201（3.5%），不能把版本覆盖率当准确率。固定 SDK 回归 6 份报告各重复 10 次无漂移；本轮未改评分。174 份 AI 复核报告中 140 份有图（80.5%）。
- 升级点击：现有报表尚无埋点，缺口明确；有效订阅为 0。Google Search Console 已登录页面在本轮读取超时，展示、点击和收录数不可得；不能用 `site:` 查询替代。线上 robots/sitemap 均 200；近几天声称 Googlebot/Bingbot 的公开页面请求大量 200、所查范围无 5xx，但 User-Agent 可能伪造，不据此断言官方抓取或收录。线上旧版注册仍可自动登录，本地验证码/邮件验证方案因第三方服务配置未就绪未发布；近 7 天注册页本身 0 浏览，不能把当前无注册归因于表单失败。

## 今日交付与质量门槛

- Skill 报告首屏增加“评测我的项目”入口，保留“查看评测依据”为主操作；新入口用现有匿名聚合 CTA 事件观察，不新增追踪身份。相关组件单测断言目标与操作顺序。`ui-styling` 的移动端、点击面积、语义链接与键盘焦点规则用于本次组件改动，没有引入新的视觉特效。
- 本机 typecheck、lint 通过；网站测试 170 通过、1 个既有跳过，SDK 12/12，榜单 3/3；生产 build 成功。沙箱最初拒绝测试的本地监听（EPERM），原测试命令在允许本地监听的权限下通过。构建期间 Better Auth 在缺少本机生产密钥的静态数据收集阶段记录默认密钥警告，构建退出码 0；线上候选版使用与旧版逐字节相同的生产 `.env`，并重新通过 typecheck、lint、网站评测测试 154/154、组件定向测试 2/2、SDK 12/12、榜单 3/3 和生产 build。
- 生产候选版前两次 build 因 1.8 GiB 物理内存及构建 scope 限额触发 OOM，旧版 Web/Worker 始终在线。核对磁盘空间与独立目标不存在后，在服务器创建专用 `/opt/skillsupermarket-build.swap`（2 GiB、0600，未写入开机挂载）；第三次 build 在 1.1 GiB scope 与 640 MiB Node 堆限制下成功，`BUILD_ID` 和路由产物存在。保留此非持久 swap 供后续构建，不执行清理或停用。
- V2EX 和 Hacker News 官方规则均明确禁止直接发送 AI 生成发帖文字；未发帖、未建号、未刷票。Product Hunt 当前展示准则也不鼓励目录/报告类产品作为专题发布；没有把渠道名单当成果。

## 后续检查点与唯一支持事项

业务组件与测试已提交为 `d6b4ae7`。北京时间约 16:55 原子切换至 `/opt/releases/skillsupermarket-20260926-own-eval-cta`，旧版 `/opt/releases/skillsupermarket-20260925-report-telemetry` 完整保留。切换前待处理及运行中的评测任务均为 0；当天两份采集快照已同步至候选版，生产 `.env` 与旧版一致。新建数据库备份 `/opt/backups/skillsupermarket-pre-20260926-own-eval-cta.dump`（2,708,585 字节、0600），`pg_restore --list` 可读；这不是完整恢复演练。

发布后 Web/Worker 均 online 且 `/proc` 实际工作目录指向新 release；`/api/health` 的数据库与 Judge 均 ready。公网报告 GET 200 且实际 HTML 包含“评测我的项目”→`/evaluate`；首页、登录、sitemap、robots、Google 验证文件均 200。`npm run seo:check` 全项通过，涵盖真实报告与分享图、218 个 sitemap URL、匿名评测 HTTP 401、受保护页面登录跳转、榜单快照；线上固定 SDK 12/12，无模型调用和评分漂移。TLS 验证返回 0，HSTS 与 nosniff 存在；采集和安全 timer active、采集 service 最近结果 success。一个公网 HEAD 探测曾超时，随后本机与公网 GET 均正常，未复现用户请求故障。首轮 SEO 命令会话未返回明确结果，带 90 秒界限的完整复测退出码 0，未靠放宽检查过关。

安全巡检只出现预期 `CODE_INTEGRITY_CHANGED`；核对 release 和 396 文件摘要 `cb1a2e70d6412fadcc97c2d4bbf274a62fb2d10b4a4c7dca071aebde54a0d05c` 后只更新代码基线。账号、授权公钥、监听端口基线哈希前后不变，17:55 复查 healthy。根分区使用 47%，可用约 20 GiB；内存 available 约 1.2 GiB。部署完成不代表用户增长，7–14 天继续观察同一详情页入口假设、真实首评及搜索来源，样本不足时不判胜负。唯一需要用户支持的长期项目仍是启用并安全提供验证码与发信服务配置，之后才能发布已写好的防滥用注册流程；本轮不以该项阻塞详情页的低风险承接改动。

参考：[V2EX 社区规则](https://v2ex.com/about)、[Hacker News 发布准则](https://news.ycombinator.com/newsguidelines.html)、[Product Hunt 展示准则](https://help.producthunt.com/en/articles/9883485-product-hunt-featuring-guidelines)。
