# Skill Supermarket 日报 · 2026-09-20

**本日解决的是一个直接影响搜索获客和首次理解的入口混乱：搜索用户仍可能落到已废弃的 `openai/skills`，而站内真实评测没有说明“格式合格”和“当前可安装来源”的区别。** 已完成一篇结论优先的 OpenAI / Anthropic Skills 对比入口，并接入真实报告、首次评测、指南内链和 sitemap；当前尚未上线和观察，因此不宣称流量或转化增长。

## ① 真实数据与当前瓶颈

北京时间 2026-09-20 首次只读复盘：总账号 1；新增账号 D1/D7/D30 均为 0；页面浏览 1/28/163，评测方法页 0/1/8，登录注册页 0/0/7，评测 CTA 0/0/6，指南正文 0/0/5，指南 CTA 与继续阅读 D1/D7/D30 均为 0。D7 来源中自然搜索 2、GitHub 0、社区 0。没有搜索后台曝光数据，不能把低访问断言为零收录。

用户评测任务 D1/D7/D30 为 0/0/15，15 次全部完成、失败 0；D30 只有一个活跃评测账号且存在复评，样本不足以外推成功率。库存 198、已有评测 187、累计报告 209，覆盖率 94.4%。最新 3.16.0 报告 4/198 是版本覆盖，不是准确率；最新 AI 报告 171 份，其中 137 份有图（80.1%），当天 3/3 有图且无无效图。

当前唯一主瓶颈仍是**目标用户发现与进入首次价值不足**。本轮假设：面向正在搜索 OpenAI Skills、Anthropic Agent Skills 和 Codex/Claude 接入差异的开发者，用“现行入口怎么选”回答迁移问题，并让读者无需注册即可进入一份真实评测，会比继续堆通用目录页更有机会产生合格访问和下一步动作。

## ② 一手证据与产品判断

- [OpenAI Skills](https://github.com/openai/skills) 已由维护方明确标记为废弃，当前 Codex 示例迁移到 [OpenAI Plugins](https://github.com/openai/plugins)。
- [Anthropic Agent Skills](https://github.com/anthropics/skills) 仍提供 Claude Code、Claude.ai 与 API 接入入口，但仓库也明确示例不能替代关键任务的独立测试。
- [Agent Skills 开放规范](https://agentskills.io/specification) 统一 `SKILL.md` 结构和 frontmatter；格式通过不等于运行时正确、安全或跨宿主兼容。
- 站内 `openai/skills` 3.16.0 报告识别 24 个有效且有实质正文的 `SKILL.md`，但只深入评审一个候选，其余没有逐一运行时测试；仓库废弃状态应优先于报告分数。

完整可追溯对照已更新到[竞品能力矩阵](../competitor-capability-matrix.md)。本轮只采用“现行来源优先”的一项机制，不做 Star 排行、不复制外部评分，也不把官方身份写成安全背书。

## ③ 本日交付与验证

新增指南 `OpenAI Skills vs Anthropic Agent Skills：2026 该选哪个入口？`，包含：

- 结论优先的 Codex / Claude 入口选择；
- 五项响应式、可访问对比表；
- 共同 `SKILL.md` 格式能证明和不能证明的边界；
- 旧 `openai/skills` 真实报告入口、24 份文件的证据范围与流程图解释；
- 正常、错误、不应触发和恶意内容四类验收，以及首次评测 CTA；
- 官方来源、canonical、Article/Breadcrumb JSON-LD、指南内链与 sitemap 自动覆盖。

验证结果：针对指南、内部链接、canonical、Google 验证和 sitemap 的 10 项测试全部通过；typecheck、lint、完整评测/安全回归 166 pass / 1 Linux-only skip、Next 生产 build 全部通过。构建仍出现本机既有 BetterAuth 默认 secret 提示，生产认证配置未改变；没有降低断言或跳过失败。

## ④ 线上与发布状态

截至本文件记录时，改动已通过本地生产门禁，尚待提交、推送和隔离发布。发布前必须重新确认免密 SSH、可恢复数据库备份和至少一个未被进程引用的回滚 release；切换后验证健康接口、Web/Worker 实际目录、定时采集、指南、真实报告、sitemap 和安全基线。

## ⑤ 七天验证窗口

- 成功信号：该指南获得真实访问，且指南继续阅读、真实报告点击或评测 CTA 至少一项大于 0。
- 调整条件：有访问但所有下一步点击仍为 0，先改首屏承诺和 CTA，不继续堆同类文章。
- 停止条件：7 天访问仍为 0，停止扩写相同意图内容，优先修搜索发现、内链或一次经确认的合规分发入口。
- 指标边界：IndexNow 或 sitemap 受理不等于收录；页面浏览不等于独立用户；后台评测和验收流量不计真实转化。

## ⑥ 本轮唯一需要支持的事项

当前无新增支持事项。任何目录 PR、帖子、评论或私信仍在发送前请求确认；本轮不以未发送的外部分发冒充增长。
