# Skill Supermarket 日报 · 2026-09-21

**本日继续解决同一个获客阻碍：昨天发布的 Agent Skills 高意图指南尚未被真实用户看到，而首页仍有少量现有访问。** 本轮不评价指南内容好坏，也不新增文章；只把同一指南提升到首页，让已有访问和搜索爬取路径能够直接进入现行入口对比与真实报告。

## ① 真实数据与算法指标

采样时间：北京时间 2026-09-21 00:10，生产数据库只读报表。流量按北京时间自然日，D1 尚不足 11 分钟；用户、任务和新增 Skill 为滚动窗口，不能视为同一批用户的严格漏斗。

| 指标 | D1 | D7 | D30 |
| --- | ---: | ---: | ---: |
| 新增账号 | 0 | 0 | 0 |
| 页面浏览 | 0 | 27 | 166 |
| 评测方法页浏览 | 0 | 1 | 8 |
| 登录/注册页浏览 | 0 | 0 | 7 |
| 评测 CTA | 0 | 0 | 6 |
| 指南正文浏览 | 0 | 0 | 5 |
| 指南 CTA / 继续阅读 | 0 / 0 | 0 / 0 | 0 / 0 |
| 用户评测任务 | 0 | 0 | 12 |
| 完成 / 失败 | 0 / 0 | 0 / 0 | 12 / 0 |
| 活跃评测账号 | 0 | 0 | 1 |

总账号 1；首次完成评测账号 D1/D7/D30 均为 0，D30 重复评测账号 1。D7 来源为自然搜索 3、GitHub 0、社区 0；没有搜索后台曝光数据，不能断言零收录。有效订阅、免费额度使用与耗尽账号均为 0；升级点击仍未埋点，明确列为不可得。

库存 198、D1/D7/D30 新增 1/9/21；最近采集日期 9 月 20 日，00:10 尚未开始当天采集不判定为故障。有报告 187、累计 209，覆盖率 94.4%；薄描述 36、30 天未更新 15。D7 自动覆盖任务完成 9/9、失败 0，仅是后台可靠性。

评测器 3.16.0 的最新报告覆盖 4/198（2.0%），其他版本 183；这不是准确率。最新 AI 报告 171、有图 137（80.1%），D1 2/2 有图、无效图 0；流程/时序/架构为 66/25/46，恢复 3/6。没有固定金标的新结果，不宣称算法质量变化。

## ② 主瓶颈与可证伪假设

路径聚合显示 D7 首页 8 次浏览、指南目录 1 次，而昨天 19:04 上线的新指南没有记录。观察窗口不足一天，但证据已足以判断目前卡在**发现入口**，而不是“读了之后不点击”。

14:38 增量采样显示 D1 仅新增 1 次页面浏览，D7 首页仍为 8 次、新指南仍无流量行，指南 CTA 与继续阅读仍为 0。专题入口上线约 5 小时，不能用这个窗口提前判定首页承接失败；新增浏览也没有经过目标路径。

19:58 再次读取同一聚合口径，页面、指南、CTA、注册和用户评测指标均未变化。专题入口上线约 10.5 小时，仍没有目标用户行为证据；不把“无新增事件”解释成内容无价值，也不重置七天停止条件。

继续昨天同一假设：把“OpenAI Skills 已迁移、Claude Agent Skills 仍是现行入口”的指南放到首页现有流量路径，能让目标用户无需先进入指南目录就看见决策价值。成功条件仍为七天内新指南出现真实访问，且报告点击、继续阅读或评测 CTA 至少一项大于 0；有访问无点击则改首屏承诺，七天仍无访问则停止扩写同意图内容并转向索引或经确认的合规分发。

## ③ 本日交付与验证

首页新增一个单一专题入口，明确展示：Codex 应看 OpenAI Plugins、Claude 应看 Agent Skills、共同格式是 `SKILL.md`，并链接昨天的完整对比指南。入口不称安全认证、不给安装量排行，也不复制外部文案；指南页面浏览会继续使用现有隐私友好聚合口径，不新增个人追踪。

首个候选通过 typecheck、lint 和 24 项针对性回归，但完整套件中注册保护用例在约五分钟后让 OTP 过期，实际 400、预期 200；其余 166 项通过、1 项 Linux-only 跳过。首页候选随后已从工作树回滚，当时未提交或部署。

根因是该用例同时检查“未验证用户不得登录”和“随后用同一 OTP 验证”，却依赖五分钟真实时钟；完整套件中的密码哈希在 CPU 竞争下超过 OTP TTL。过期边界已有相邻独立用例覆盖。本轮将会话门禁用例的 OTP 固定到足够长的测试有效期，不改变生产五分钟 TTL、不跳过断言，也不把简单重跑当作通过。修正后需先通过认证用例，再重新应用首页候选并执行完整门禁。

修正后的认证文件 15/15 通过；重新应用首页候选后，typecheck、lint、针对性 24/24、完整回归 167 pass / 1 Linux-only skip / 0 fail、Next 生产 build 全部通过。构建仍显示本机既有 BetterAuth 默认 secret 提示；生产环境与认证功能未改，不把该提示隐藏或当作生产认证验收。候选只新增首页入口和测试，不改算法、权重、权限、数据库、额度或依赖。代码已提交为 `e06b568`。

生产发布再次从当前线上 release 做隔离叠加，未带入尚未配置验证码服务的注册功能。生产环境内的 typecheck、lint、153 项生产基线回归、SDK build、Next build、依赖哈希和候选首页真实渲染全部通过后，才切换正式流量。

等待指南观察窗口期间，没有转向新文章或算法小修。14:40 重新核实既定的 Awesome MCP DevTools 单项目录 PR：上游仍为固定 HEAD，贡献规则与 `Testing Tools` 分类未变，两个品牌精确词的全状态 Issue/PR 搜索均为 0 且未报告不完整；一行补丁仍可干净应用并且只新增 1 行。PR 中四个生产链接均匿名 HTTP 200。完整证据更新在[单点分发包](../distribution/2026-09-09-mcp-devtools.md)；尚未创建 fork、分支或 PR，不把准备工作称为触达。

这份文档型交付仍执行了项目门禁：typecheck、lint、完整回归 167 通过 / 1 项 Linux-only 跳过 / 0 失败，Next 生产 build 通过。首次测试与构建分别被本机沙箱在创建临时 IPC、写入 `.next/trace-build` 时以 EPERM 拦截，均发生在业务断言或编译前；使用项目所需权限原样重跑后通过，没有吞异常或减少覆盖。本机既有 BetterAuth 默认 secret 构建提示仍保留；没有修改生产认证配置。

20:00 补充搜索发现证据：从首页专题上线时间起聚合 Nginx access log 的 User-Agent 分类，Googlebot 样式请求 28 次（首页 2、robots 6、新指南 0），Bingbot 样式请求 12 次（首页 1、新指南 0），YandexBot 样式请求 2 次（robots 1、新指南 0）。统计不输出 IP 或完整 User-Agent，也未做反向 DNS 验证，因此只能说明这些声明为爬虫的请求已能取得首页，不能证明搜索引擎已经收录或理解页面。

三个公开精确搜索查询返回本站首页、指南目录和其他已收录页面，但未返回新指南 URL；这同样不是 Search Console 覆盖率。结合首页已被爬取、robots 允许、sitemap 已包含该 URL、Article/Breadcrumb 结构化数据与 canonical 均已存在，本轮没有可复现的站内阻断可修，当前更符合新 URL 的抓取排队/索引延迟。继续改 title、结构化数据或重复提交 IndexNow 没有证据支持，暂不实施。

## ④ 线上、故障与安全基线

00:12 首次复核：健康接口 database/Judge ready、评测器 3.16.0；当时 release 为 `skillsupermarket-20260920-agent-skills-guide`；采集和安全 timer active，最近 service result 均为 success。没有发现需要抢占主体工作的线上异常。

09:31 已切换到 `/opt/releases/skillsupermarket-20260921-home-guide`。独立复核确认首页专题入口及顺序、指南、真实报告、sitemap、Google 验证文件、HSTS/nosniff、离线 SDK 3.16、Web/Worker PM2 工作目录和两个 systemd timer 全部通过；安全巡检 healthy。切换前数据库备份为 `/opt/backups/skillsupermarket-pre-20260921-home-guide.dump`，2,599,839 字节、权限 0600，并通过 `pg_restore --list`；旧 release 保留可回滚。临时 swap 已清除，根分区 40G、已用 9.0G、可用 29G、占用 25%。

本机到 GitHub 的 HTTPS 连接在推送阶段曾两次失败（一次 HTTP/2 framing error、一次 443 连接失败）；09:35 使用 HTTP/1.1 重试成功，功能提交 `e06b568` 和本日报提交 `eb128cc` 均已推送到 `origin/main`。

20:02 轻量复核：健康接口 database/Judge ready、评测器 3.16.0，Web/Worker 仍运行于 `skillsupermarket-20260921-home-guide`；采集与安全 timer active、最近 service result 均为 success，安全巡检 healthy。没有发布新应用 release。

## ⑤ 未来七天与下一实现项

保持同一指南观察窗口，不新增同主题文章。上线后按路径与来源区分首页访问、新指南访问、报告点击和评测 CTA；内部验收不计用户增长。若出现一次真实目标访问，再依据实际下一步行为决定是补强报告承接还是首次评测；没有行为证据不改评分算法。

搜索发现下一检查点为上线满 48 小时后的爬虫路径与公开搜索结果，以及原定七天用户行为窗口；期间不重复改页面。若 48 小时后首页已持续被搜索爬虫获取而指南仍无请求，再把 Google Search Console 的 URL 检查/提交状态作为唯一缺失证据；没有后台证据前不宣称零收录。

## ⑥ 唯一需要用户支持事项

当前唯一需要用户决策：是否按[已核实的目标、标题、正文和一行补丁](../distribution/2026-09-09-mcp-devtools.md)，向 `punkpeye/awesome-mcp-devtools` 提交这 **1 个**收录 PR？授权只覆盖该目标与现有内容，不扩大到批量投稿、推广评论或私信。
