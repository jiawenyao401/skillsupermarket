# GitHub → 公开报告 → 首次评测入口实验

日期：2026-09-08。目标用户：正在为 Claude Code / AI 应用选择 Skill 或 MCP Server 的开发者，以及希望理解项目接入风险的维护者。

## 为什么做这件事

同日生产基线 D7：PV 105，评测 CTA 2，新增注册 0；来源中 GitHub 11，自然搜索和社区均为 0。这里的 PV 是浏览次数，不是独立用户；现有统计不能把 11 次访问归因到本仓库 README，也不能关联访问者与后续注册。

自有 GitHub 仓库简介仍为 `skill market`，Website 指向 www 地址；README 的首屏直接进入运行时和功能清单，没有具体的免登录报告入口，且写死了已过期的引擎版本。对于已有的 GitHub 访问渠道，用户需先自行寻找报告才能判断产品是否有用。

21:39 的只读细分基线：GitHub 来源 11 次浏览、0 次 CTA；目标 SurfSense 报告无记录，目标 MCP 选型指南只有 1 次站内来源浏览、0 次 CTA。不能把全站指南 4 次浏览都归到目标文章。

站点允许索引、sitemap 可访问，IndexNow 当日已有成功受理记录。这不等于已收录；未取得搜索后台数据，不把自然搜索为 0 写成零收录。旧 Vercel 域名本次请求未成功，重定向状态未证实。

## 单一假设与交付范围

假设：把 GitHub 入口从技术说明改为“明确采用问题 → 直接看真实报告 → 评测自己的项目”，可以让已经到达仓库的目标访客更容易理解首次价值，并产生下一步评测意图。

- README 第一屏新增免登录 SurfSense 报告、既有 Claude Code MCP 选型指南和登录后评测入口；继续承接 9 月 4 日的指南实验，不再扩写一批文章。
- 不展示硬编码分数、库存或“全量已评测”宣称。示例不是项目推荐；图示不是执行轨迹；报告并非安全认证。
- 当前版本改为源码链接，提醒历史报告不会随部署自动更新。
- 自有 GitHub 简介明确 AI Skill / MCP 评测和证据，Website 统一为已核实的正式域名；不修改第三方仓库，不发布 Issue、PR、评论或营销消息。
- 原计划只修改 README 与记录、不做应用发版。发布前真实浏览器验收发现：390px 视口下报告整页宽度为 672px。根因是证据区的隐式单列最小内容宽度受到长命令影响，不是允许容器内横滑的时序图；因此先补齐承接页修复，再发布引导，未放宽验收。
- 应用修复仅涉及 `components/EvaluationReport.tsx` 的显式可收缩列宽、证据换行与对应回归：不通过全页裁切掩盖内容，也不再截断证据。保持既有视觉系统，不改评分、认证、依赖、数据库与权益。
- 线上整页扩展验收另发现 768px 时导航撑宽至 780px（不是证据修复引入的布局）：`app/layout.tsx` 仅将桌面导航与折叠菜单的切换断点一起从 md 移到 lg。目标是在首次访问的中等屏幕保留完整导航与登录入口，不增加新功能或更换视觉体系。

## 观察与停止条件

观察 9 月 9–15 日七个完整北京时间自然日，9 月 16 日复盘；9 月 4 日指南自身的原观察期限不重置。

- 查询既有 `traffic_daily` 中 GitHub 来源的浏览、评测 CTA，以及 `/skill/modsettersurfsense` 和目标指南的浏览与 CTA；同时参考全站新增注册、首次完成评测，不做跨表个人归因。
- 前置行为信号：目标报告或指南获得真实 GitHub 来源访问，且 GitHub 来源评测 CTA 大于 0。它只说明链路有人继续，不证明 README 因果效果或获得新用户。
- 若七天无目标页面访问，停止继续润色 README，优先推进有明确目标和内容、经用户发送确认的合规分发；若有访问但无 CTA，复核报告是否真正帮助选型以及进入评测的阻力。
- 无法区分 README、其他 GitHub 页面和被浏览器隐藏的来源；不新增访客 ID、跨站 Cookie 或虚构唯一访客转化率。小样本只用于下一步决策，不声称统计显著提升。
- 验收流量必须拦截 `/api/events`，不注册测试账号、不创建评测任务，不把自测算作增长。
- 等待窗口期间的下一工作：核实一个相关社区的发布规则，准备包含真实示例、局限和明确目标的分发文案，发送前按授权边界确认；不因为等数据又转回无关技术小修。

## 验收记录

本机已通过 typecheck、lint、生产 build；评测测试 136 通过、1 项 Linux 专属跳过，榜单 3/3。新增回归保留超长路径、证据全文和换行，并确认不把证据里的 HTML 当作脚本。

真实报告与超长文档标签/路径/证据夹具分别在 320/375/390/414/768/1280px 浏览器验证：无整页溢出、证据不裁切且全文保留。390px 修复前报告宽度 672px，候选变为 390px。截图经人工查看；不把浏览器合成样本计入真实增长。

报告修复提交 `7be06b6`、导航修复提交 `643ff2c` 均已推送 main；生产隔离提交依次为 `aec082a`、`c9f7e4c`，不包含尚未开通服务的注册保护。

导航候选在真实匿名页面的静态快照上验证，仅替换这两个断点、加载本机正式构建 CSS：320/375/390/414/639/640/767/768/900/1023/1024/1100/1280/1440px 全部无整页溢出；1024px 以下原生折叠菜单可用 Enter 打开并访问指南、评测和账户链接，1024px 起桌面导航正常。未登录态验收不扩大为已登录或管理员浏览器验收。

22:16 正式站切换至 `c9f7e4c`，生产 typecheck/lint/build、122/122 测试、真实 PostgreSQL 回归、榜单与三组既有基准全部通过。备份 2,043,472 字节、归档可读，前版保留可回滚。Web/Worker 实际进程目录均为新 release；采集 timer active、最近 service success，SEO 和匿名鉴权边界通过。

发布后真实匿名浏览器在 320/375/390/414/768/1023/1024/1280px 完整复核：报告与指南整页均不溢出；报告锚点、时序图、菜单显隐与 Enter 操作、原项目复评登录返回路径正常，页面脚本错误 0。拦截统计请求 32 次，评测 POST 0 次；未注册、未写评测报告、无模型调用。旧示例报告仍为 3.12.0，运行引擎仍为 3.13.0，没有用重评制造进展。

安全差异只有预期代码摘要，核对精确 release 后登记摘要；账号、公钥、端口基线不变，22:22:41 复检 healthy。磁盘 64% → 70%（12G 可用），来自保留两次隔离构建，没有删除任何文件。后续应把整页断点验收前置到发布前，避免只验证单组件后再补发。

GitHub 简介与 Website 更新已由 API 回读确认；既有主题保持不变。README 五个产品入口全部核实，本地 LICENSE、源码版本链接存在。README 与本记录同批推送 main；远程核对以 README blob SHA 和 GitHub 渲染后的五个链接为准。

本次只是交付和链路验收，没有新的用户增长结论。下一检查点为合规分发包准备及上述真实行为窗口，不改算法、不继续堆同类指南。
