# Judge 信任边界回归：2026-08-29

目标：阻止不可信项目内容伪造 Judge 提示边界，并避免已知凭证形态被发送给第三方 Judge 或写入公开报告。评分权重不变。

## 固定恶意样本

同一输入同时在名称、描述和 README 中放入内部闭合标记、聊天模板标记与模拟 API Key。

| 指标 | Rubric 3.2.0 基线 | Rubric 3.3.0 候选 |
| --- | ---: | ---: |
| 元数据 ASCII 闭合标记数量 | 3 | 1（仅系统合法标记） |
| README ASCII 闭合标记数量 | 2 | 1（仅系统合法标记） |
| 聊天模板边界数量 | 1 | 0 |
| 提示字符数 | 1,924 | 1,924 |
| 模拟凭证进入提示 | 是 | 否 |

固定测试集 `judge trust-boundary set 1.0.0` 还验证：防护是确定性的；非凭证恶意文本不扩张提示长度；图示字段中的模拟 GitHub Token 会在公开输出前脱敏。

## 正常公开项目复评

样本：`github/github-mcp-server`，同一模型、temperature 0，每个候选连续调用两次。

| 指标 | 基线 | 候选 |
| --- | ---: | ---: |
| 首次总分 | 83 | 83 |
| 重复总分 | 88 | 83 |
| 分数漂移 | 5 | 0 |
| 图示 | sequence | sequence |
| 两次调用总耗时 | 14.79 秒 | 15.20 秒 |

候选未改变正常样本首分和图示，实测延迟增加 0.41 秒（约 2.8%，在外部模型波动范围内），固定提示长度没有增加。决定进入发布；若线上 Judge 失败率或延迟持续恶化则回滚到前一 release。
